"""This module is for testing exceptions"""

class A():
  def exceptFinal_GlobalFunc(self):
    self.x = 'alma'
    
    try:
      self.y = 2
      
      print(self.x)
      raise DummyError("An error occured.")
      print(globalVar)

      global globalVar

    except NameError as ex:
      int_localVar = os.getpid()
      print(ex)
    except:
      os.getenv("PYTHONPATH")
    finally:
      globalVar = "Just a string."


def reraise_tryexcept_GlobalFunc():

  try:
    raise a.a2.a2m.SuperOfDummyError("Error", 42)
  except:
    raise


def tryexcept_GlobalFunc(strParam):

  try:
    raise DummyError("An error occured.")
    print(globalVar)

    global globalVar

  except(NameError):
    int_localVar = system.maxsize()
  except:
    os.getenv(strParam)
    globalVar = True
